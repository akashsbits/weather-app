{
  "name": "weather-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "npx prettier --write ."
  },
  "devDependencies": {
    "prettier": "^2.8.1",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "axios": "^1.2.2"
  }
}
